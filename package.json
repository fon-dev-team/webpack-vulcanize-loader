{
  "name": "webpack-vulcanize-loader",
  "version": "0.2.0",
  "description": "Webpack Polymer Vulcanization loader",
  "main": "index.js",
  "files": [
    "index.js"
  ],
  "scripts": {
    "test": "istanbul cover _mocha -- -R mocha-junit-reporter --reporter-options mochaFile=../reports/tests.xml",
    "test:watch": "mocha -R spec -w",
    "preversion": "npm run test && istanbul check-coverage"
  },
  "keywords": [
    "webpack",
    "polymer",
    "vulcanize",
    "loader"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.1.2",
    "mocha-junit-reporter": "^1.12.1"
  },
  "dependencies": {
    "loader-utils": "^0.2.16",
    "vulcanize": "^1.14.8"
  }
}
